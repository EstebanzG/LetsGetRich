{% extends 'base.html.twig' %}

{% block title %}Lets Get Rich !{% endblock %}

{% block body %}
    {% block header %}
        {% include "partials/_header-back.html.twig" %}
    {% endblock %}
    <div class="container d-flex justify-content-center align-item-center">
        <div class="col-md-3 col-sm-1 col-lg-4 bg-white rounded-5 d-flex justify-content-center" >
            <div class="w-75">
                {{ form_start(newBudget) }}
                <h2 class="mb-3 mt-3 text-center">Nouvelle catégorie de budget</h2>
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success mt-1">
                        {{ message }}
                    </div>
                {% endfor %}
                <div class="form-group">
                    {{ form_label(newBudget.Category) }}
                    {{ form_widget(newBudget.Category) }}
                    <div class="form-error">
                        {{ form_errors(newBudget.Category) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(newBudget.LimitAmount) }}
                    {{ form_widget(newBudget.LimitAmount) }}
                    <div class="form-error">
                        {{ form_errors(newBudget.LimitAmount) }}
                    </div>
                </div>
                <div class="mb-3 mt-3 text-center">{{ form_row(newBudget.submit) }}</div>
                {{ form_end(newBudget) }}
            </div>
        </div>
    </div>
{% endblock %}